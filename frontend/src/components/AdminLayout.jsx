import { useState, useEffect } from 'react';
import axios from 'axios';
const BASE = (import.meta.env.VITE_API_URL || 'http://localhost:3001/api').replace(/\/$/, '');
const API = BASE + '/mongo-products';
const PLACEHOLDER = 'https://via.placeholder.com/400x400/0a0a0a/D4AF37?text=M';

export default function AdminLayout(){
  const [products,setProducts]=useState([]);
  const [auth,setAuth]=useState(null);
  const [phone,setPhone]=useState('');
  const [search,setSearch]=useState('');
  const [filterBrand,setFilterBrand]=useState('All');
  const [showAdd,setShowAdd]=useState(false);
  const [editing,setEditing]=useState(null);
  const [form,setForm]=useState({name:'',brand:'',category:'Sneakers',buyingPrice:'',sellingPrice:'',stock:'1',size:'40-45',color:'',image:''});
  const [loading,setLoading]=useState(false);
  const ALLOWED={'254706631292':'Samuel','254723808067':'Monicah'};
  const norm=(p)=>{let x=p.replace(/\D/g,'');if(x.startsWith('0'))x='254'+x.slice(1);if(x.startsWith('7'))x='254'+x;return x;};
  useEffect(()=>{const s=localStorage.getItem('monique_admin');if(s){try{setAuth(JSON.parse(s))}catch{}}},[]);
  const fetchProducts=async()=>{setLoading(true);try{const r=await axios.get(API);setProducts(r.data.products||r.data||[])}catch{}finally{setLoading(false)}};
  useEffect(()=>{if(auth)fetchProducts()},[auth]);
  const brands=['All',...new Set(products.map(p=>p.brand).filter(Boolean))];
  const filtered=products.filter(p=>(p.name+' '+p.brand).toLowerCase().includes(search.toLowerCase())&&(filterBrand==='All'||p.brand===filterBrand));
  const totalValue=products.reduce((s,p)=>s+(Number(p.sellingPrice||p.price||0)*Number(p.stock||1)),0);
  const getImg=(p)=>p.image||p.image_url||PLACEHOLDER;
  const handleSave=async()=>{if(!form.name||!form.sellingPrice)return alert('Name & Price required');setLoading(true);try{const payload={name:form.name,brand:form.brand,category:form.category,buyingPrice:Number(form.buyingPrice||0),sellingPrice:Number(form.sellingPrice),price:Number(form.sellingPrice),stock:Number(form.stock||1),size:form.size,color:form.color,image:form.image,image_url:form.image};if(editing)await axios.put(API+'/'+editing._id,payload);else await axios.post(API,payload);setShowAdd(false);setEditing(null);setForm({name:'',brand:'',category:'Sneakers',buyingPrice:'',sellingPrice:'',stock:'1',size:'40-45',color:'',image:''});fetchProducts()}catch{alert('Error')}finally{setLoading(false)}};
  const handleDelete=async(id)=>{if(!confirm('Delete?'))return;await axios.delete(API+'/'+id);fetchProducts()};
  if(!auth)return(<div className="min-h-screen bg-black flex items-center justify-center p-4"><div className="bg-white p-10 rounded- w-full max-w-sm"><h1 className="font-black text-3xl text-center">MONIQUE</h1><p className="text-center text-xs tracking-[0.3em] text-[#D4AF37] font-bold">ADMIN VAULT</p><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="0706 631 292" className="w-full border-2 p-4 rounded-2xl mt-8 font-bold"/><button onClick={()=>{const a=ALLOWED[norm(phone)];if(a){const d={name:a,phone:norm(phone)};localStorage.setItem('monique_admin',JSON.stringify(d));setAuth(d)}else alert('Unauthorized')}} className="w-full bg-black text-[#D4AF37] py-4 rounded-2xl mt-4 font-black">ENTER →</button></div></div>);
  return(<div className="min-h-screen bg-[#fcfaf5] text-black"><div className="bg-black text-white sticky top-0 z-50 border-b border-[#D4AF37]/30"><div className="max-w- mx-auto px-6 py-4 flex justify-between items-center"><div className="flex items-center gap-3"><div className="w-10 h-10 bg-[#D4AF37] rounded-xl flex items-center justify-center font-black text-black">M</div><div><div className="font-black">MONIQUE INVENTORY - {auth.name}</div><div className="text- text-[#D4AF37] font-bold">{products.length} PRODUCTS • GOLD</div></div></div><div className="flex gap-2"><button onClick={()=>{setEditing(null);setForm({name:'',brand:'',category:'Sneakers',buyingPrice:'',sellingPrice:'',stock:'1',size:'40-45',color:'',image:''});setShowAdd(true)}} className="bg-[#D4AF37] text-black px-6 py-2.5 rounded-full font-black text-sm">+ ADD</button><button onClick={()=>{localStorage.removeItem('monique_admin');setAuth(null)}} className="bg-white/10 px-5 py-2.5 rounded-full text-xs font-bold">Logout</button></div></div></div><div className="max-w- mx-auto p-6"><div className="grid grid-cols-3 gap-4 mb-6"><div className="bg-black text-white rounded-2xl p-6"><div className="text-[#D4AF37] text-xs font-bold">PRODUCTS</div><div className="text-4xl font-black">{products.length}</div></div><div className="bg-white rounded-2xl p-6 border"><div className="text-gray-400 text-xs font-bold">VALUE</div><div className="text-2xl font-black">KSh {totalValue.toLocaleString()}</div></div><div className="bg-white rounded-2xl p-6 border"><div className="text-gray-400 text-xs font-bold">STATUS</div><div className="text-sm font-black text-green-600">● Live</div></div></div><div className="bg-white rounded-2xl p-3 border flex gap-3 mb-6"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search..." className="flex-1 bg-[#fcfaf5] rounded-full px-5 py-2.5 outline-none"/><div className="flex gap-2">{brands.map(b=><button key={b} onClick={()=>setFilterBrand(b)} className={`px-4 py-2 rounded-full text-xs font-black ${filterBrand===b?'bg-black text-[#D4AF37]':'bg-[#fcfaf5]'}`}>{b}</button>)}</div></div><div className="grid grid-cols-2 md:grid-cols-4 gap-4">{filtered.map(p=><div key={p._id} className="bg-white rounded-2xl border overflow-hidden group hover:shadow-xl transition"><div className="aspect-square bg-[#fcfaf5] relative overflow-hidden"><img src={getImg(p)} onError={e=>e.target.src=PLACEHOLDER} className="w-full h-full object-cover group-hover:scale-110 transition duration-500"/><div className="absolute top-2 left-2 bg-black/90 text-[#D4AF37] text- font-black px-2 py-1 rounded-full">{p.brand}</div></div><div className="p-3"><div className="font-black text-sm truncate">{p.name}</div><div className="text- text-gray-400">{p.category} • {p.size}</div><div className="font-black mt-1">KSh {p.sellingPrice||p.price}</div><div className="flex gap-2 mt-2"><button onClick={()=>{setEditing(p);setForm({name:p.name,brand:p.brand||'',category:p.category||'Sneakers',buyingPrice:p.buyingPrice||'',sellingPrice:p.sellingPrice||p.price||'',stock:p.stock||1,size:p.size||'40-45',color:p.color||'',image:p.image||p.image_url||''});setShowAdd(true)}} className="flex-1 bg-black text-white py-1.5 rounded-full text-xs font-bold">Edit</button><button onClick={()=>handleDelete(p._id)} className="flex-1 bg-gray-100 py-1.5 rounded-full text-xs font-bold">Del</button></div></div></div>)}</div></div>{showAdd&&<div className="fixed inset-0 bg-black/80 z-[100] flex items-center justify-center p-4"><div className="bg-white rounded- w-full max-w-lg p-6 max-h- overflow-y-auto"><h2 className="font-black text-xl mb-4">{editing?'EDIT':'ADD'} PRODUCT</h2><div className="space-y-3"><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Name" className="w-full bg-[#fcfaf5] p-3 rounded-xl font-bold"/><div className="grid grid-cols-2 gap-2"><input value={form.brand} onChange={e=>setForm({...form,brand:e.target.value})} placeholder="Brand" className="bg-[#fcfaf5] p-3 rounded-xl"/><input value={form.category} onChange={e=>setForm({...form,category:e.target.value})} placeholder="Category" className="bg-[#fcfaf5] p-3 rounded-xl"/></div><div className="grid grid-cols-2 gap-2"><input value={form.buyingPrice} onChange={e=>setForm({...form,buyingPrice:e.target.value})} type="number" placeholder="Buying" className="bg-[#fcfaf5] p-3 rounded-xl"/><input value={form.sellingPrice} onChange={e=>setForm({...form,sellingPrice:e.target.value})} type="number" placeholder="Selling" className="bg-[#fcfaf5] p-3 rounded-xl border-2 border-[#D4AF37]/30"/></div><input value={form.image} onChange={e=>setForm({...form,image:e.target.value})} placeholder="Image URL" className="w-full bg-[#fcfaf5] p-3 rounded-xl"/><button onClick={handleSave} className="w-full bg-black text-[#D4AF37] py-3 rounded-xl font-black">{editing?'UPDATE':'ADD'}</button><button onClick={()=>setShowAdd(false)} className="w-full py-2 text-sm">Cancel</button></div></div></div>}</div>)
}